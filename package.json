{
  "name": "generate-clone",
  "version": "1.0.2",
  "description": "create a directory with the project name passed in your current location and fill it with the necessary files to start a basic JS project",
  "main": "index.js",
  "bin": {
    "my-command": "./bin/my-command"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
